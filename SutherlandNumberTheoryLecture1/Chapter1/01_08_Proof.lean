import Mathlib

/-!
# Proof of Theorem 1.8

The lecture defers the proof of Ostrowski's theorem to Problem Set 1. The
project's theorem statement in `01_08_Theorem` already records the relevant
Mathlib declarations, so this proof blob contributes no additional local API.

Use `Rat.AbsoluteValue.equiv_real_or_padic` directly at call sites.
-/
