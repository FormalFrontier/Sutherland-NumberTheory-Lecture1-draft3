## Accomplished
- Claimed issue `#133` and verified its prerequisites were already merged and reflected in `progress/status.json`.
- Added `SutherlandNumberTheoryLecture1/Chapter1/01_28_Proposition.lean`, formalizing Proposition 1.28 with a direct `minpoly`-based scaffold.
- Added `minpoly_dvd_of_fractionRing_aeval_eq_zero` using `minpoly.isIntegrallyClosed_dvd` and `isIntegral_iff_exists_map_eq_minpoly` using `minpoly.isIntegrallyClosed_eq_field_fractions'`.
- Wired the new module into `SutherlandNumberTheoryLecture1/Chapter1.lean`, updated `progress/status.json`, and ran `lake exe cache get` plus `lake build` successfully.

## Current frontier
- `Chapter1/01_28_Proposition` is complete on this branch and ready to publish as the resolution of issue `#133`.

## Overall project progress
- The Stage 3.1 Chapter 1 scaffold now covers the core integrality vocabulary through Definition 1.19, Proposition 1.20, Proposition 1.22, and Proposition 1.28.
- The remaining nearby integrality follow-ons are still mostly `references_attached`, with proofs/examples queued behind these exact-match scaffolds.

## Next step
- Merge the PR for `#133`, then claim `#148` (`Chapter1/01_28_Proof`) or `#149` (`Chapter1/01_25_Proposition`) depending on which dependency chain the queue exposes next.

## Blockers
- None.
