## Accomplished
- Claimed human-oversight issue `#772` after merging the two ready auto-merge PRs queued ahead of new work.
- Removed the duplicate `eq_zero_or_one_of_nonneg_of_pow_eq_self` declaration from `SutherlandNumberTheoryLecture1/Chapter1/01_05_Proof.lean` and kept a single public version in `SutherlandNumberTheoryLecture1/Chapter1/01_05_Corollary.lean`.
- Updated `progress/status.json` and `UPSTREAMING.md` so their declaration references point at the surviving helper location.
- Ran `lake exe cache get`, validated `progress/status.json` with `jq empty`, and verified the change with `lake build SutherlandNumberTheoryLecture1.Chapter1`.

## Current frontier
- Issue `#772` is complete locally and ready to publish through the standard PR flow.

## Overall project progress
- The Chapter 1 human-oversight cleanup wave continues to trim redundant declarations and simplify the formalization surface without changing the mathematical API.
- Ready PRs `#781` and `#782` were set to auto-merge before taking new work, keeping `main` as unblocked as branch policy allows.

## Next step
- Commit the `#772` cleanup, create the PR with auto-merge enabled, and then return to the remaining human-oversight queue (`#773`, `#774`, `#778`, `#779`).

## Blockers
- None.
